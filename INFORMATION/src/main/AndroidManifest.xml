<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="thd.bd.sms">

    <uses-permission android:name="android.permission.WAKE_LOCK"></uses-permission>

    <!-- 这个权限用于进行网络定位-->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"></uses-permission>
    <!-- 这个权限用于访问GPS定位-->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"></uses-permission>
    <!-- 用于访问wifi网络信息，wifi信息会用于进行网络定位-->
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE"></uses-permission>
    <!-- 获取运营商信息，用于支持提供运营商信息相关的接口-->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"></uses-permission>
    <!-- 这个权限用于获取wifi的获取权限，wifi信息会用来进行网络定位-->
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE"></uses-permission>
    <!-- 用于读取手机当前的状态-->
    <uses-permission android:name="android.permission.READ_PHONE_STATE"></uses-permission>
    <!-- 写入扩展存储，向扩展卡写入数据，用于写入离线定位数据-->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"></uses-permission>
    <!-- 访问网络，网络定位需要上网-->
    <uses-permission android:name="android.permission.INTERNET" />

    <uses-permission android:name="android.permission.WRITE_SETTINGS"
        tools:ignore="ProtectedPermissions" />
    <!-- SD卡读取权限，用户写入离线定位数据-->
    <uses-permission android:name="android.permission.MOUNT_UNMOUNT_FILESYSTEMS"
        tools:ignore="ProtectedPermissions"></uses-permission>

    <uses-permission android:name="android.permission.READ_CONTACTS"/>
    <uses-permission android:name="android.permission.WRITE_CONTACTS"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/MAppTheme1"
        android:name=".application.SMSApplication">

        <meta-data
            android:name="com.baidu.lbsapi.API_KEY"
            android:value="uk0WQ6V5uYiITHhgwqjZ4w6mhk4sfeUi" >
        </meta-data>

        <activity android:name=".activity.MainActivity"
            android:launchMode="singleInstance"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <activity android:name=".activity.BSIActivity"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar">

        </activity>

        <activity android:name=".activity.ReplyMessageActivity"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar">

        </activity>

        <activity android:name=".activity.BDContactActivity"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar">

        </activity>

        <activity android:name=".activity.BDTimeActivity"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar">

        </activity>

        <activity android:name=".activity.MainCenterActivity"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar">

        </activity>

        <activity android:name=".activity.StatelliteStatusActivity"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar">

        </activity>

        <activity android:name=".activity.LocActivity"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar">

        </activity>

        <activity android:name=".activity.FriendLocationMapActivity"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar">

        </activity>

        <activity android:name=".activity.RDLocationSetActivity"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar">

        </activity>

        <activity android:name=".activity.ReportSetActivity"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar">

        </activity>


        <activity android:name=".activity.FriendLocationDetailActivity"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar">

        </activity>





        <!-- 连续定位服务 -->
        <service android:name=".service.CycleLocService">
            <intent-filter>
                <action android:name="thd.bd.sms.CycleLocService"/>
            </intent-filter>
        </service>

        <!-- 连续报告基类 -->
        <service android:name=".service.CycleReportService">
            <intent-filter>
                <action android:name="thd.bd.sms.CycleReportService"/>
            </intent-filter>
        </service>
        <!-- 连续rd位置报告 -->
        <service android:name=".service.CycleReportRDLocService">
            <intent-filter>
                <action android:name="thd.bd.sms.CycleReportRDLocService"/>
            </intent-filter>
        </service>

        <!-- 连续rn位置报告 -->
        <service android:name=".service.CycleReportRNLocService">
            <intent-filter>
                <action android:name="thd.bd.sms.CycleReportRNLocService"/>
            </intent-filter>
        </service>
        <!-- 连续短报文状态报告服务 -->
        <service android:name=".service.CycleReportStatuService">
            <intent-filter>
                <action android:name="thd.bd.sms.CycleReportStatuService"/>
            </intent-filter>
        </service>
        <!-- 连续短报文sos报告服务 -->
        <service android:name=".service.CycleReportSOSService">
            <intent-filter>
                <action android:name="thd.bd.sms.CycleReportSOSService"/>
            </intent-filter>
        </service>


    </application>

</manifest>